# Anonymous function definition with topicalizer
my $func_<<id>> = function_def($f, <<owner>>, <<quoted_name>>, [ { name => '_' } ], <<returns>>, sub {
    my ($scope, $self, $this, undef, $args, $ret) = &args_v1;
    my $ins = $args->{_};
    <<statements>>
    return $ret;
});
