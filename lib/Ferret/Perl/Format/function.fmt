$context->set_property('<<name>>' => do {
    my $func = Ferret::Function->new($f, name => '<<name>>');
    $func->{code} = sub {
        my ($arguments, $scope) = @_;
        $scope = Ferret::Scope->new($f, parent => $scope);
        <<statements>>
    };
    $func;
});
