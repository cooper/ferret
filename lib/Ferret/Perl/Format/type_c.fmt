FF::typedef($scope, $class, '<<name>>', sub {
    my ($ins, $create_can, $transform) = @_;
    FF::typedef_check($scope, $class, $ins,
        conditions => <<conditions>>,
        equal_to => <<equal_to>>
    ) ? $ins : undef;
}, <<lazy>>);
