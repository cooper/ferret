# Method event '<<name>>' definition
{
    my $func = Ferret::Function->new($f,
        name      => 'default',
        is_method => 1
    );
    <<arguments>>
    $func->{code} = sub {
        my ($self, $arguments, $from_scope, $scope, $return) = @_;
        <<statements>>
        return $return;
    };
    $methods[<<id>>] = Ferret::Event->new($f,
        name         => '<<name>>',
        default_func => [ undef, $func ]
    );
}
