use Ferret::Core::Operations qw(<<operations>>);
my $result = do {
    my @funcs;
    my $scope = my $context = $f->get_context('<<package>>');
    do 'CORE.frt.pm' or die "Core error: $@" unless '<<package>>' eq 'CORE';
    undef;

    <<upper_content>>
    <<lower_content>>
};
