use Ferret::Core::Operations qw(<<operations>>);
my $result = do {
    my ($file_scope, $context) = get_context($f, '<<package>>');
    my $scope = $file_scope;
    load_core('<<package>>');

    <<upper_content>>
    <<lower_content>>
};
