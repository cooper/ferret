use Ferret::Core::Operations qw(<<operations>>);
my $result = do {
    my @funcs;
    my $scope = my $context = $f->get_context('<<package>>'); undef;
    Ferret::space($context, 'CORE') or die 'CORE error';

    <<includes>>
    <<upper_content>>
    <<lower_content>>
};
