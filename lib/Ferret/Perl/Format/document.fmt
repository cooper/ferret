use Ferret::Core::Operations qw(<<operations>>);
my $result = do {
    my ($scope, $context) = FF::get_context($f, '<<package>>');
    FF::load_core('<<package>>');

    <<upper_content>>
    <<lower_content>>
};
