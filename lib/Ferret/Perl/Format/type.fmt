typedef($scope, <<owner>>, '<<name>>', sub {
    my ($ins, $create_can, $transform) = @_;
    state $anchor = \0 + 0;
    typedef_check($scope, $scope, $ins, $anchor,
        conditions => <<conditions>>,
        equal_to => <<equal_to>>
    ) ? $ins : undef;
}, <<lazy>>);
