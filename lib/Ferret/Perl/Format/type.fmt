FF::typedef($scope, '<<name>>', sub {
    my ($ins, $create_can, $transform) = @_;
    FF::typedef_check($scope, $ins,
        conditions => <<conditions>>,
        equal_to => <<equal_to>>
    ) ? $ins : undef;
});
