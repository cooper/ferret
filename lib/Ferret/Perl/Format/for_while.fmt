{
    my $loop_ret =
    FF::while_true($f, $scope, sub { <<condition>> }, sub {
        my ($scope, $ret_func) = @_;
        <<body>>
    }, <<pos>>);
    return $ret_func->($loop_ret) if $loop_ret;
}
