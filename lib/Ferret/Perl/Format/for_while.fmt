{
    my $loop_ret =
    while_true($f, $scope, sub { <<condition>> }, sub {
        my ($scope, $ret_func) = @_;
        <<body>>
    }, $pos->(<<pos>>));
    return $ret_func->($loop_ret) if $loop_ret;
}
