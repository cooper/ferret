# Function '<<name>>' definition
{
    my $func = $funcs[<<id>>] = Ferret::Function->new($f, name => '<<name>>');
    <<arguments>>
    $func->{code} = sub {
        my ($_self, $arguments, $from_scope, $scope, $return) = @_;
        my $self = $_self || $self;
        <<statements>>
        return $return;
    };
}
